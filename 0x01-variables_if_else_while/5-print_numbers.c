#include <stdio.h>
/**
 * main - print numbers
 *
 * Description: the numbers are in base 10 and they start from 0
 *
 * return: always 0 (success)
 */
int main(void)
{
	int i;

	for (i = 0; i > 0; i++)
	{
		printf("%d\n", i);
	}
	return (0);
}
