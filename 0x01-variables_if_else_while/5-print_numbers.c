#include <stdio.h>
/**
 * main - print numbers
 *
 * Description: the numbers are in base 10 and they start from 0
 *
 * Return: always 0 (success)
 */
int main(void)
{
	int i;

	for (i = 0; i < 10; i++)
	{
		printf("%d", i);
	}
	return (0);
}
