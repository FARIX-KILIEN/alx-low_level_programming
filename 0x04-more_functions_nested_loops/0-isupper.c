#include "main.h"

/**
 * main - checks if parameter is an uppercase character.
 *
 * Description: uppercase and lowercase checking
 *
 * Return: 1 if is an uppercase character, 0 in other case.
 */
int main(void)
{
	if (c >= 65 && c <= 90)
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
